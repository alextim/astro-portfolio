---
export interface Props {
  contacts: Contacts;
  socialLinks: SocialLinks;
  address: Address;
}

import Container from '../Container.astro';
import LegalInfo from './components/LegalInfo.astro';
import Social from './components/Social.astro';

const {
  contacts: { foundingDate },
  socialLinks,
  address,
} = Astro.props as Props;
---

<footer class="py-4 text-sm bg-light  dark:bg-black">
  <Container>
    {socialLinks && (
      <div class="flex flex-wrap justify-center gap-8 my-12 w-full">
        <Social socialLinks={socialLinks} />
      </div>
    )}
    <div class="inline-flex items-center justify-center w-full lg:justify-start">
      <LegalInfo foundingDate={foundingDate} name={address?.name} />
    </div>
  </Container>
</footer>
