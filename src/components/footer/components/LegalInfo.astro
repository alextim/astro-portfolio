---
export interface Props {
  foundingDate?: string;
  name: string;
  text?: string;
}

const { foundingDate, name, text = '' } = Astro.props as Props;

const currentYear = new Date().getFullYear();
let foundingYear = '';
if (foundingDate) {
  const y = new Date(foundingDate).getFullYear();
  if (y !== currentYear) {
    foundingYear = `${y}-`;
  }
}

const dot_text = text ? `. ${text}` : '';
---

<div><span class="mr-3">© {foundingYear}{currentYear}</span><span>{name}{dot_text}</span></div>
