---
export interface Props {
  value: Record<string, any>;
}

const { value } = Astro.props as Props;
const space = process.env.NODE_ENV === 'development' ? 2 : undefined;
---

<script type='application/ld+json' set:html={JSON.stringify(value, null, space)}>

</script>
