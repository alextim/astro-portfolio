<button id="menu-toggle" class="menu-toggle" type="button" aria-label="Navigation bar toggle" aria-expanded="false">
  <div class="toggle-line"></div>
  <div class="toggle-line"></div>
  <div class="toggle-line"></div>
</button>

<style>
  .menu-toggle {
    @apply relative  m-0 flex h-12
    w-12 cursor-pointer
    flex-col items-center
    justify-center
    border-none bg-transparent
    lg:pointer-events-none lg:hidden;
    .toggle-line {
      @apply my-1 h-1 w-6
      transform
      rounded-full
      bg-white opacity-50 transition
      duration-300;
    }
    &:hover .toggle-line {
      @apply opacity-100;
    }

    &.open {
      .toggle-line {
        &:nth-child(1) {
          transform: translateY(0.75rem) rotate(45deg);
        }

        &:nth-child(2) {
          opacity: 0;
        }

        &:nth-child(3) {
          transform: translateY(-0.75rem) rotate(-45deg);
        }
      }
    }
  }
</style>
